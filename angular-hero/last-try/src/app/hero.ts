// interface is only a contract that does not instnatiate a new class
// so only checks to see if the properties are there or not
// class does the same thing as interface but is able to instantiate a new object
// based on the blueprint
export class Hero {
  id: number;
  title: string;
}
